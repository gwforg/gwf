
Test of regex transformations.

Old solution

@list foos foo1 foo2 foo3
@transform bars foo(.*) bar\1 foos

@comment

New solution -- although we shouldn't even define @list objects in the future

<%!

foos = ['foo1','foo2','foo3']
bars = [re.sub(r'foo(.*)',r'bar\1',foo) for foo in foos]

%>

@list foos2 ${ ' '.join(foos) }
@list bars2 ${ ' '.join(bars) }
