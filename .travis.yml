language: python
sudo: required
addons:
  hostname: cibox
  hosts:
    - cibox
python:
  - "3.4"
  - "3.5"
install:
- make init
- make install-slurm
- pip install coveralls==1.1
script: make test && make integration-test
after_success:
- coveralls
